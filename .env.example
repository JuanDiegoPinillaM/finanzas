# MongoDB
MONGODB_URI=
# JWT
JWT_SECRET=
JWT_EXPIRATION=1d

# App
PORT=3000
FRONTEND_URL=http://localhost:4200

# SMTP (configurar con tu proveedor de correo)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Finanzas App" <noreply@finanzas.com>
